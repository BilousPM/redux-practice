const initialState = {
  counter: 0,
  step: 1,
};

export const counterRredcer = (state = initialState; action) => {
  switch (action.type) {
    case value:
      
      break;
  
    default:
      return state
      break;
  }
};
